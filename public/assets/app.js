const audio=document.getElementById('audio');
const playButtons=[document.getElementById('mainPlay'),document.getElementById('listenBtn'),document.getElementById('stickyPlay')];
const progress=document.getElementById('progress'), volume=document.getElementById('volume');
const currentTime=document.getElementById('currentTime'), duration=document.getElementById('duration'), stickyTime=document.getElementById('stickyTime');
const title=document.getElementById('currentTitle'), artist=document.getElementById('currentArtist'), ticker=document.getElementById('tickerTitle'), stickyTitle=document.getElementById('stickyTitle'), stickyArtist=document.getElementById('stickyArtist');
const eq=document.getElementById('eq');
const tracks=[['Тестовый эфир','Онлайн Радио Хит'],['Популярный трек','Новый артист'],['Свежая премьера','Онлайн Радио Хит']];
let index=0, repeat=false;
function fmt(s){if(!Number.isFinite(s))return '0:00';return Math.floor(s/60)+':'+String(Math.floor(s%60)).padStart(2,'0')}
function sync(){const playing=!audio.paused;document.querySelectorAll('[data-action="play"]').forEach(b=>b.classList.toggle('is-playing',playing));playButtons.forEach(b=>b?.classList.toggle('is-playing',playing));eq.classList.toggle('playing',playing);document.querySelectorAll('.play-shape,.icon.play').forEach(el=>el.classList.toggle('pause',playing));}
async function toggle(){if(audio.paused){try{await audio.play()}catch(e){console.warn('Воспроизведение недоступно:',e)}}else audio.pause();sync()}
playButtons.forEach(b=>b?.addEventListener('click',e=>{e.preventDefault();toggle()}));
document.querySelectorAll('[data-action="repeat"]').forEach(b=>b.addEventListener('click',()=>{repeat=!repeat;document.querySelectorAll('[data-action="repeat"]').forEach(x=>x.classList.toggle('active',repeat));}));
document.querySelector('[data-action="prev"]').addEventListener('click',()=>change(-1));
document.querySelector('[data-action="next"]').addEventListener('click',()=>change(1));
function change(step){index=(index+step+tracks.length)%tracks.length;const t=tracks[index];title.textContent=t[0];artist.textContent=t[1];ticker.textContent=t[0];stickyTitle.textContent=t[0];stickyArtist.textContent=t[1];audio.currentTime=0;toggle()}
audio.addEventListener('play',sync);audio.addEventListener('pause',sync);audio.addEventListener('loadedmetadata',()=>duration.textContent=fmt(audio.duration));audio.addEventListener('timeupdate',()=>{currentTime.textContent=fmt(audio.currentTime);stickyTime.textContent=fmt(audio.currentTime);progress.value=audio.duration?(audio.currentTime/audio.duration*100):0});audio.addEventListener('ended',()=>{if(repeat){audio.currentTime=0;audio.play().catch(()=>{})}else change(1)});progress.addEventListener('input',()=>{if(audio.duration)audio.currentTime=progress.value/100*audio.duration});volume.addEventListener('input',()=>audio.volume=Number(volume.value));audio.volume=.8;
const songs=[['Ночной город','KARAT'],['Jumanji','KARAT & МЛАДШИЙ'],['Ты такая славная','Вячеслав Калинин'],['Просто я ищу тебя','Новый артист'],['Летуаль','PELIKUANA'],['Когда расцветает сирень','Андрей Додонов']];
function row(song,i){return `<div class="mini-track"><strong>${i+1}</strong><button class="mini-cover" aria-label="Слушать ${song[0]}"><span>${song[0][0]}</span></button><div class="track-copy"><b>${song[0]}</b><small>${song[1]}</small></div><button class="mini-play" aria-label="Воспроизвести"><span class="play-shape"></span></button><span class="mini-plays">${(1200-i*137).toLocaleString('ru-RU')}</span></div>`}
document.getElementById('popular').innerHTML=songs.slice(0,3).map(row).join('');
document.getElementById('chartRows').innerHTML=songs.slice(0,5).map((s,i)=>`<div class="chart-row"><strong>${i+1}</strong><span class="chart-cover">${s[0][0]}</span><div><b>${s[0]}</b><small>${s[1]}</small></div><span class="wave"><i></i><i></i><i></i><i></i><i></i></span><em>${(3200-i*410).toLocaleString('ru-RU')}</em></div>`).join('');
document.getElementById('releasesGrid').innerHTML=songs.map((s,i)=>`<article class="release-card"><button class="release-cover"><span>${s[0][0]}</span><i class="play-icon"></i></button><div><small>ПРЕМЬЕРА</small><h3>${s[0]}</h3><p>${s[1]}</p></div></article>`).join('');
document.querySelectorAll('.mini-play,.mini-cover,.release-cover').forEach((b,i)=>b.addEventListener('click',()=>{const s=songs[i%songs.length];title.textContent=s[0];artist.textContent=s[1];ticker.textContent=s[0];stickyTitle.textContent=s[0];stickyArtist.textContent=s[1];toggle()}));
const sticky=document.getElementById('sticky');let last=scrollY;addEventListener('scroll',()=>{const down=scrollY>last;sticky.classList.toggle('visible',down&&scrollY>280);last=scrollY},{passive:true});
document.getElementById('mailForm').addEventListener('submit',e=>{e.preventDefault();document.getElementById('mailMsg').textContent='В тестовом стенде форма работает локально. На WordPress подключим обработчик.'});

const previewNav=document.querySelector('.site-header nav');const previewMenu=document.querySelector('.site-header .menu');if(previewNav)previewNav.id='mainNav';if(previewMenu)previewMenu.id='menuBtn';const previewCss=document.createElement('link');previewCss.rel='stylesheet';previewCss.href='/assets/preview-fixes.css';document.head.appendChild(previewCss);const previewScript=document.createElement('script');previewScript.src='/assets/preview-fixes.js';document.body.appendChild(previewScript);
