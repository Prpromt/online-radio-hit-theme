import { createServer } from "node:http";
import { readFile } from "node:fs/promises";
import { join } from "node:path";

const root = join(process.cwd(), "public");
const workerAsset = async (path, contentType) => { const body = await readFile(join(root, path)); return new Response(body, { headers: { "content-type": contentType, "cache-control": "no-store" } }); };
const page = async (path) => { const body = await readFile(join(root, path)); let html = body.toString(); html = html.replace('/assets/app.js?v=11','/assets/app.js?v=12'); html = html.replace('/assets/web-polish.css?v=5','/assets/web-polish.css?v=5'); return new Response(html,{headers:{"content-type":"text/html; charset=utf-8","cache-control":"no-store"}}); };
export default { async fetch(request) { const url = new URL(request.url); if (url.pathname === '/assets/app.js') return workerAsset('assets/app.js','application/javascript; charset=utf-8'); if (url.pathname === '/artist-levels' || url.pathname === '/artist-levels/') return page('artist-levels.html'); if (url.pathname === '/embed/' || url.pathname === '/embed') return page('embed/index.html'); if (url.pathname === '/embed-code/' || url.pathname === '/embed-code') return page('embed-code/index.html'); if (url.pathname.endsWith('.css')) return workerAsset(url.pathname.replace(/^\//,''),'text/css; charset=utf-8'); if (url.pathname.endsWith('.js')) return workerAsset(url.pathname.replace(/^\//,''),'application/javascript; charset=utf-8'); if (url.pathname.endsWith('.svg')) return workerAsset(url.pathname.replace(/^\//,''),'image/svg+xml'); if (url.pathname === '/' || url.pathname === '/index.html') return page('index.html'); try { const file = await readFile(join(root,url.pathname.replace(/^\//,''))); return new Response(file,{headers:{"cache-control":"no-store"}}); } catch { return page('index.html'); } } };